# Production API (Render). Loaded automatically by `vite build`.
# Override per deployment in Vercel/host env if this URL changes.
VITE_API_BASE_URL=https://luna-senior-project.onrender.com
VITE_USE_MOCK_AUTH=false
